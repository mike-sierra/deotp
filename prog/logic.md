---

## Introduction

In this unit you learn:

- how to make decisions about what code to execute

- about _truth_, and special `True` and `False` boolean values

- about _existence_, and the special `None` value

- how to compare an object's _identity_ rather than its value

- how to compare numbers and strings
